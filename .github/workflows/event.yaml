name: Push Events
on:
  schedule:
    - cron: '*/5 * * * *' # every five minutes
jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "This is a scheduled event running every five minutes"

    
    
    
  

 
